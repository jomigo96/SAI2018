# Ficheiros Simulink

+ `model.mat` - constantes que os esquemas usam.
+ `model.slx` - equema do modelo da aeronave e controlador.
+ `model_2015_re.slx` - esquema com as redundâncias e injeção de falhas.

Correr 
```matlab
load model.mat
```
antes de efectuar simulações